<Window x:Class="CodexUsageCompanion.CollapsedToggleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="28"
        Height="28"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ShowActivated="False"
        Topmost="False"
        Focusable="False"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True">
    <Button x:Name="ToggleButton"
            Width="28"
            Height="28"
            Background="Transparent"
            Foreground="{StaticResource SecondaryTextBrush}"
            BorderThickness="0"
            Focusable="False"
            IsTabStop="False"
            AutomationProperties.Name="展开 Codex 用量组件"
            ToolTip="展开组件"
            Click="OnToggleClicked">
        <Button.Template>
            <ControlTemplate TargetType="{x:Type Button}">
                <Border x:Name="Chrome"
                        Width="28"
                        Height="28"
                        Background="{TemplateBinding Background}"
                        CornerRadius="9">
                    <Path Width="18"
                          Height="18"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Stretch="Uniform"
                          StrokeThickness="1.5"
                          StrokeStartLineCap="Round"
                          StrokeEndLineCap="Round"
                          StrokeLineJoin="Round"
                          Stroke="{TemplateBinding Foreground}"
                          Data="M 12,2 L 12,12 M 6.6,5.3 A 8,8 0 1 0 17.4,5.3" />
                </Border>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter TargetName="Chrome" Property="Background" Value="#252528" />
                        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
                    </Trigger>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter TargetName="Chrome" Property="Background" Value="#303034" />
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>
        </Button.Template>
    </Button>
</Window>
